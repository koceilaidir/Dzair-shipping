# Copier en `.env` (jamais dans git) — dev local via docker compose.
DATABASE_URL=postgresql://postgres:postgres@db:5432/dzair
# Postgres managé (Supabase…) : DATABASE_SSL=1 obligatoire.
DATABASE_SSL=0
JWT_SECRET=changer-moi-64-caracteres-aleatoires-minimum-xxxxxxxxxxxxxxxxxxxxxxxx
JWT_EXPIRES=12h
# Origines autorisées (séparées par des virgules). Vide = tout (dev seulement).
CORS_ORIGIN=
PORT=3000
